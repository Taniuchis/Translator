%h2 Text from Spanish to English
= form_for :translator, :url => url_for(:controller => 'translator', :action => 'result'), method: 'get' do |f|
	.field
		= f.text_area :text

	.actions
		= f.submit 'Translate'